// Write a function that takes the base and height of a triangle and return its area.

function triArea(base, height) {
  const result = (base * height) / 2;
  return result;
}

const area = triArea(7, 4);
console.log(area);
